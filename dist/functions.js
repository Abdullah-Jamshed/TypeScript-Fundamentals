"use strict";
// FUNCTION
function addNumber(x, y) {
    return x + y;
}
// void
function message(message) {
    console.log(message);
}
message("Hello World");
const interfaceFuncAdd1 = (x, y) => {
    return x + y;
};
const interfaceFuncSub1 = (x, y) => {
    return x - y;
};
