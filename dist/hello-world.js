"use strict";
/**
 * @param {string} param_name -  value
 * * comments
 * ? query
 * ! danger
 * link to function {@link RefFunction}
 * {@link https://github.com GitHub}
 */
function FirstFunction(name, age) {
    console.log("name ==>> ", name);
    console.log("age ==>> ", age);
    return age > 18;
}
function RefFunction() { }
